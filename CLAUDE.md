# frenship

Ship software with your AI frens. An AI-native SDLC: resumable pipelines, agents, playbooks, and conventions. `global/` symlinks into `~/.claude/` to apply across workspaces; see `README.md` for the install steps and the layout.
